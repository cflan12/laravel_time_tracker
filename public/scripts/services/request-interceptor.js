/*
(function() {

	'use strict';

	angular
		.module('timeTracker')
		.factory('myInterceptor', ['$log', function($log) {
			$log.debug('$log request-interceptor factory injector');

			var myInterceptor = {
				//var interceptor = saveHours(data);

			};

			return myInterceptor;

		}]);
})();
*/